# ============================================================
# Dev-only dependencies — testing. Not needed to run the app itself.
# Install with (from backend/, inside the venv):
#     pip install -r requirements.txt -r requirements-dev.txt
# Then run the suite with:
#     pytest
# ============================================================
pytest>=8.0
httpx>=0.27          # required by fastapi.testclient.TestClient, used in test_api_smoke.py
